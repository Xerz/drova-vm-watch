version: "3.8"

services:
  obs-rtmp:
    build: .
    container_name: obs-rtmp
    env_file: .env
    ports:
      - "${RTMP_PORT}:1935"
    volumes:
      - ${RECORD_PATH}:/recordings
      - ./tokens.conf:/etc/nginx/tokens.conf
    restart: unless-stopped